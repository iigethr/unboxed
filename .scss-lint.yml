# Default application configuration that all configurations inherit from.

linters:
  EmptyLineBetweenBlocks:
    enabled: false

  ImportantRule:
    exclude:
      - "app/assets/stylesheets/lib/columns/**"
      - "app/assets/stylesheets/lib/overriders/**"

  NestingDepth:
    enabled: false

  SelectorDepth:
    enabled: false

  SpaceBeforeBrace:
    enabled: true
    style: space
    allow_single_line_padding: true

  StringQuotes:
    enabled: true
    style: double_quotes
