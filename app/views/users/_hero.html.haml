/* Hero */

.hero
  .hero-box
    .hero-item.altai-center
      = image_tag "icons/favorite.png", title: "#{application_configuration["title"].to_s}", alt: "hero image"
    .hero-item.wide
      %p.extra-large.text-center.wide.left.blue-500-cl= @winner
.separator.extra-small
